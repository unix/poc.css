{
  "name": "docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "release": "pnpm --filter poc.css build && pnpm build && wrangler pages deploy",
    "astro": "astro",
    "preinstall": "npx only-allow pnpm"
  },
  "dependencies": {
    "astro": "^6.4.4",
    "poc.css": "workspace:*"
  }
}
